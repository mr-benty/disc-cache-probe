name: multi-env-deploy
on: workflow_dispatch
jobs:
  deploy-a:
    runs-on: ubuntu-latest
    environment: env-a
    steps:
      - run: echo "deployed to env-a"
  deploy-b:
    runs-on: ubuntu-latest
    environment: env-b
    steps:
      - run: echo "deployed to env-b"
